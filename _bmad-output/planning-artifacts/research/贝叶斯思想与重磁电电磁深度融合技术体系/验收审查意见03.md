# 《贝叶斯思想与重磁电电磁深度融合技术体系》整改验收审查意见03

审查日期：2026-07-27
本次事实快照：2026-07-28T03:30:14Z
审查依据：《审查意见03》第九节 10 条复审放行硬门槛
路径基准：除非另有说明，本文路径均相对于仓库根。

## 一、结论

**验收结论：有条件不通过。**

锁定环境和 WP9 四专项门禁已经完成本地修复：`uv 0.11.29` 按 `uv.lock` 重建 Python 3.11.15 环境，统一 runner 的完整测试实跑为 `433 passed、1 skipped`；四份 review 已分别绑定正演参考一致性、SBC、三层离散化收敛和 CI 失败阻断证据，退化模板、Rejected、身份误标及坏结构均以退出码 4 拒绝。WP7 v6/v4 签核重放、WP8 跨平台语义重放和 WP9 保存证据包本地审计均为 `passed`，但这不等于总门禁或发布验收通过。

当前 `final-gates-v1.json` 的本次锁定环境复跑总状态为 `blocked`，本地 live gate 仅剩一项阻断，另有远程闭环待执行：

1. 回环 33 活动指针已纠正，但新指针尚未重新取得六角色确认，也未绑定到经授权的 WP5 签核根；WP5 当前报 `root drift ACTIVE_MANIFEST`。
2. WP8/WP9 及关键整改资产已获得本轮 Git/推送授权并完成精确暂存核对，但本事实快照尚未取得目标提交的远程 Ubuntu/Windows 门和 Sigstore/Rekor 证明。

WP7 曾因 v3/v3 签核和归档缺少 `pod_basis` 阻断，现统一 runner 使用已验证的 `signoff-v4.json` 对 v6/v4 重放通过。WP8 曾报 `method-validation:semantic-replay-mismatch`，现仅排除平台/机器/CPU 与墙钟字段，并按显式科学门槛约束浮点容差；环境漂移正例和决策相关篡改负例均已通过。

因此，不得表述为“整改已通过复审”“WP9 已通过远程证明”或“四名独立人类专家已签署”；可以据实表述为“WP7/WP8/WP9 当前本地门已通过，WP5 与远程闭环仍阻断总验收”。

## 二、2026-07-27 事实复核

### 2.1 Git 与远程覆盖

- 本地分支为 `main`，HEAD=`50e25166f8897f0fc6e82cbadbc3c4f0a98c14d5`；本地远程跟踪引用 `refs/remotes/origin/main`=`9e2c2e9ca53b6c20eb5cb3f3f1f7ae348a9ac6bc`（本轮未 fetch，不声称为远端实时状态），本地领先 1 个提交且工作区有未提交修改。
- 精确暂存以 persistence plan 的 `required_git` 为白名单：195 个 required 路径全部在索引中解析，实际形成 120 个相对 HEAD 的 staged 变更；无白名单外 staged 路径。三个大型 supplement NPZ 继续忽略并由 CI 确定性生成，不进入 Git。
- 已生成研究目录下的 `validation/wp9/persistence-plan-v1.json`：WP9 manifest 的 36 个成员全部通过存在性、字节数和 SHA-256 闭包；另列 162 个不重复执行/源码/测试控制输入。合计 195 个 `required-git` 路径，其中 132 个当前内容尚未进入冻结基线 HEAD；三个 supplement NPZ 明确为忽略的确定性生成资产，两次隔离重生成彼此一致并匹配登记哈希，无未解决的“required 但 ignored”项。
- required 集合的仓库范围扫描未发现常见秘密、本机绝对路径或超过 50 MiB 的待提交文件。该结果仅表示本地准备完成；`release_ready=false`、`remote_attestation_verified=false`。
- 用户已明确授权本轮精确 commit、push 与远程双平台证明；该授权不包含伪造 WP5 六角色重签、强推或绕过分支保护。
- 已跟踪但有未提交修改的台账、执行日志、主张映射和 forward 文件不再误写成“未跟踪”。

### 2.2 锁定环境与当前门禁

- `.venv` 已从失效的跨用户解释器路径重建为仓库内 Python 3.11.15，`include-system-site-packages=false`。
- `python -m uv lock --check` 与 `python -m uv sync --frozen --extra dev` 均返回 0；未修改 `pyproject.toml` 或 `uv.lock`。
- 关键版本：`matplotlib 3.11.1`、`simpeg 0.25.2`、`discretize 0.12.0`、`numpy 2.4.6`、`scipy 1.17.1`。
- 完整 pytest：`433 passed、1 skipped、119 warnings`，退出码 0；经仓库路径归一化的完整捕获输出、逻辑 argv/cwd、起止时间和退出码已写入研究目录的 `validation/wp9/gate-logs-v1/pytest.json` 并由 manifest 保护。
- governance：通过；WP6 self-test：通过。
- WP5：阻断，`root drift ACTIVE_MANIFEST`。
- WP7：通过；`signoff-v4.json` 精确绑定 `synthetic-block-v6-20260724` 与 `do27-v4-20260724`，self-test 退出码 0。
- WP8 synthetic：通过；跨平台语义重放返回 `SYNTHETIC_COMPLETION_AUTHORIZED`，退出码 0，且仍明确 `field_validated=false`。
- WP9 保存证据包：本地审计和 manifest replay 通过；其范围明确排除上述 live gate、Git、远程证明和人类签章。

### 2.3 WP9 专项门禁修复

- 四专项分别含 `forward_unit_consistent`、`sbc_coverage_recorded`、`discretization_converged`、`gates_blocked_on_fail`，键集合不再同质。
- validator 不只读取 review 中的布尔值，还重放 WP8 九方法唯一集合、方法级检查状态、completion 投影、三个 supplement NPZ 哈希和 CI 中实际 WP9 阻断步骤。
- 工程 review 绑定 `.github/workflows/ci.yml`；其余专项绑定方法级合成验证记录。四份产物继续使用 `identity_type=automated-ai-specialist-evidence-review`，不得冒充自然人专家。
- registry 校验 canonical `PRIMARY_WP`、台账 SHA/逐行映射、精确字段、非空安全证据路径；manifest 校验精确唯一成员闭包。
- `validation/wp9/validate_wp9.py` 默认只读，仅 `--publish` 重发。阻断发布不再硬编码 package-level `Passed/Approved`。
- WP9 专项、审查治理与持久化计划的定向回归为 `33 passed`；默认 WP9 audit 与 manifest replay 均返回 0。

### 2.4 既有治理勘误保持有效

- 04 章 SHA-256=`61b215cb72e5a0393fc90793f4595aa2bcad578cbb03f046dda69adb89c56510`；05 章 SHA-256=`c6f3b01f7ecead62cec4270c9341b813615a39fa5f918ccf5b063ab72fe29909`。
- 旧 `bf4565da…` 仅为失效历史记录；不得作为当前 05 章完整性证据。
- `ACTIVE_MANIFEST` 与 `active-output.json.manifest_sha256` 均为 `9dc55bce45eb0dd499fcfd2fe3cbca6fd2319f5ab99c619ca98c04129998d3b1`。旧签核根保持不可变，因此当前 WP5 正确地 fail-closed，不能机械改写冒充重签。

## 三、硬门槛对账

| 硬门槛 | 当前结论 | 说明 |
|---|---|---|
| 1—4：P0 公式、联合模型、决策合同、观测合同 | 本地通过 | 正文整改与 WP1—WP4 证据绑定保持有效 |
| 5：锁定环境一键复现、CI 阻断 | 部分通过 | 本地锁定环境和 CI WP9 阻断已完成；远程 required-gates 未运行 |
| 6—9：链级证据、统一诊断、对抗实验、Evidence ID | 本地通过 | WP7 v6/v4 与 WP8 synthetic live replay 当前均通过；远程仍待核验 |
| 10：四专项复审、禁止外推 | 本地自动审查通过 | 专项差异化和边界门已完成；不计作四名人类专家签章 |

净结论仍为“有条件不通过”；不得用 pytest、WP7/WP8/WP9 本地通过覆盖 WP5 失败或尚未完成的远程证明。

## 四、放行条件

1. 重新绑定回环 33 活动指针根，并由原 WP5 六角色确认新根；机械改写旧签核不构成确认。
2. 保持已通过的 WP7 v6/v4 签核重放与 WP8 跨平台语义重放，不得回退到 v3/v3 或精确比较平台噪声。
3. `spec-persist-wp8-wp9-remote-attestation.md` 的本地清单、manifest 闭包和提交授权已经完成；提交后仍须验证目标 SHA 的 Ubuntu/Windows required gates。由于 WP5 当前阻断，Sigstore/Rekor 证明若被依赖门正确跳过，必须记录为真实阻断而非伪造成功。
4. 若组织规则要求自然人独立专家，四专项人类意见仍是额外硬条件；自动化 AI review 不得替代。

## 五、适用边界

当前成果可作为理论草案、局部重磁原型、WP3 物理参考回归、WP4 决策 toy、WP8 保存的合成证据历史记录和文档治理基线受控流转。任何本地小型重磁或合成证据均不得外推到全方法生产闭环、现场有效性、正式资源分类、GPU 性能或商业能力。
